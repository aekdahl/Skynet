node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
*.log
coverage/

# desktop dev (hot-reload) scratch state
.skynet-dev/

# live-preview checkouts (per-project/run worktrees the preview engine spawns)
.skynet-worktrees/

# this installation's compliance-report signing keypair (private key — never commit)
.skynet-compliance-key.json

# Playwright e2e artifacts
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
