node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store
*.log
coverage/

# desktop dev (hot-reload) scratch state
.skynet-dev/

# live-preview checkouts (per-project/run worktrees the preview engine spawns)
.skynet-worktrees/

# this installation's compliance-report signing keypair (private key — never commit)
.skynet-compliance-key.json

# Playwright e2e artifacts
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# MFA recovery material — auth/mfa.ts writes these next to the data file
# (dirname(config.dbPath), which is apps/server/ in a bare dev run). A plaintext
# file of live recovery codes must never be committed; a `git add -A` did once.
mfa-recovery.json
mfa-recovery-codes.txt
